{{!-- <div class="container-fluid">
<table>
    <tr>
        <th>ID</th>
        <th>Megnevezés</th>
        <th>Csoport</th>
    </tr>
    {{#each products}}
    <tr>
        <td>{{ID}}</td>
        <td>{{Megnevezés}}</td>
        <td>{{Csoport}}</td>
        {{!-- <td>
            <a href="edit.html?id={{id}}">Edit</a>
            <a href="delete.html?id={{id}}">Delete</a>
        </td> --}}
    {{!-- </tr>
    {{/each}}
</table>
</div> --}} 

<div class="container-fluid">
    <table class="table table-dark text-center vertical-center">
  <thead>
    <tr>
      <th scope="col">ID</th>
      <th scope="col">Name</th>
      <th scope="col">Group</th>
      <th scope="col">Operations</th>
    </tr>
  </thead>
  <tbody>
      {{#each products}}
    <tr>
      <th scope="row">{{ID}}</th>
      <td>{{Megnevezés}}</td>
      <td>{{Csoport}}</td>
      <td><a href="#" class="btn btn-secondary btn-sm">Edit</a><a href="#" class="btn btn-danger btn-sm ml-1">Delete</a></td>
    </tr>
    {{/each}}
  </tbody>
</table>
<div class="d-flex justify-content-end"><a href="#" class="btn btn-primary">New product</a></div>
</div>