<!-- New group Modal -->
<div class="modal fade" id="newgroupModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel"
    aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Új termék hozzáadása</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <form method="POST" action="/addgroup">
                    <div>Új csoport:</div>
                    <div><input type="text" name="newgroup" class="form-control form-control-lg"></div>
                    <input class="btn btn-primary mt-3" type="submit" value="Registrate">
                </form>
            </div>
        </div>
    </div>
</div>